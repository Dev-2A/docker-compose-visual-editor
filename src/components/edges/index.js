import NetworkEdge from "./NetworkEdge";
import VolumeEdge from "./VolumeEdge";
import DependsEdge from "./DependsEdge";

export const edgeTypes = {
  networkEdge: NetworkEdge,
  volumeEdge: VolumeEdge,
  dependsEdge: DependsEdge,
};
